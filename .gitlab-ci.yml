image: docker:20.10

services:
  - docker:dind

variables:
  DOCKER_TLS_CERTDIR: ""      
  TAG: $CI_COMMIT_SHA         

stages:
  - build
  - push
  - deploy

build:
  stage: build
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - echo "️  [build] Building image..."
    - cd dearwith-backend
    - docker build -t registry.gitlab.com/kimsk5453/dearwith-be:$TAG .
    - docker tag registry.gitlab.com/kimsk5453/dearwith-be:$TAG registry.gitlab.com/kimsk5453/dearwith-be:latest
    - docker images  # 이미지가 제대로 빌드되었는지 확인
  after_script:
    - docker logout $CI_REGISTRY
  artifacts:
    expire_in: 30m
    paths: [] 

push:
  stage: push
  needs:
    - build
  before_script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - echo "📦 [push] Pushing image..."
    - docker images  # 푸시 전 이미지 확인
    - docker push registry.gitlab.com/kimsk5453/dearwith-be:$TAG
    - docker push registry.gitlab.com/kimsk5453/dearwith-be:latest
  after_script:
    - docker logout $CI_REGISTRY

deploy:
  stage: deploy
  needs:
    - push
  script:
    - echo "🚀 [deploy] Deploying to EC2..."
    - apk add --no-cache openssh-client docker-compose
    - mkdir -p ~/.ssh
    - echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    - chmod 600 ~/.ssh/id_rsa
    - ssh-keyscan -H $CI_DEPLOY_HOST >> ~/.ssh/known_hosts

    - |
      ssh -i ~/.ssh/id_rsa $CI_DEPLOY_USER@$CI_DEPLOY_HOST << 'EOF'
        cd ~/DearWith-BE/dearwith-backend
        echo "TAG=$TAG" > .env
        echo "MONGO_USER=$MONGO_USER" >> .env
        echo "MONGO_PASS=$MONGO_PASS" >> .env
        docker-compose pull
        docker-compose up -d
      EOF

  only:
    - main